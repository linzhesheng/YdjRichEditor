/**
 * <% title %> Saga
 *
 */


import { takeEvery, takeLatest, put, call, select } from "redux-saga/effects";

import * as api from "services/api";
// import { makePageIndexSelector, makePageSizeSelector } from "./selectors";

// 入口
export default function* mainSaga() {
    // yield takeLatest(QUERY_LIST, queryListSaga);
    // yield takeLatest(CHANGE_PAGE_QUERY, changePageQuery);
}
