import { message } from 'antd'

/**
 * 默认的http请求配置， 默认显示错误信息提示
 */
// export const DEFAULT_HTTP_OPTION: HttpRequestOptions = {
//     onError: (err: IHttpError) => {
//         message.error(err.msg || '未知错误')
//     }
// }